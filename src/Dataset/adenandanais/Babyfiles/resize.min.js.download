require(['jquery'],function($){let windowResizeTimer;$(window).resize(function(event){clearTimeout(windowResizeTimer);windowResizeTimer=setTimeout((function(){$(window).trigger('delayed-resize',event);}),250);});});